import { UpDownColorDirective } from './up-down-color.directive';

describe('UpDownColorDirective', () => {
  it('should create an instance', () => {
    const directive = new UpDownColorDirective();
    expect(directive).toBeTruthy();
  });
});
